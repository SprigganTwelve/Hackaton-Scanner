
const fs = require('fs');
const {exec} = require('child_process');

class CodeCorrector {

    /**
     * Corrects the given code and returns the corrected version.
     * @param {string} code - The code to be corrected.
     */
    async createTemplate(rule_ids) {
        new Promise((resolve, reject) => {
            exec()
        });
    }
}
